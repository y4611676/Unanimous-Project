# ============================================================
# 共用依賴套件 — quarterly_analysis & annual_analysis
# ============================================================
# 安裝方式：pip install -r requirements.txt

pandas>=2.0.0
numpy>=1.24.0
openpyxl>=3.1.0
scikit-learn>=1.3.0
pywin32>=306        # Windows only — required for automatic Excel→PDF export in step6
